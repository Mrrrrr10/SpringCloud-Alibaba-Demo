spring:
  application:
    name: nacos-config-advance  # 表示当前应用需要向nacos配置配置中心索要nacos-config-advance的配置
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml